# CK-5875 — krispyking.com/gentleman
# Client-side (VITE_ prefix = shipped to the browser, anon key only):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (Cloudflare Pages secrets — functions/api/gentleman-results.js).
# NEVER prefix these with VITE_ or they will be bundled into the client.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
RESULTS_TOKEN=
